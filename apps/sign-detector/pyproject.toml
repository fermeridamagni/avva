[project]
name = "sign-detector"
version = "0.1.0"
description = "A sign detector built with Mediapipe and OpenCV."
requires-python = ">=3.12"
dependencies = [
  "opencv-python==4.13.0.92",
  "mediapipe==0.10.33",
  "requests==2.33.1",
  "python-dotenv==1.2.2"
]
